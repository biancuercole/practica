let palabra = "Ho la";
let cantidadletra = palabra.replace(" ", "").length;

// replace sirve para que los espacios vacios no se cuenten//

console.log("La cantidad de letras de la palabra '" + palabra + "' es: " + cantidadletra);
